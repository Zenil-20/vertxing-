# Vertxing Web — production build config (committed: this is a PUBLIC URL, not a
# secret; NEXT_PUBLIC_* values are inlined into the client bundle anyway).
# Vercel reads this at build time so the web talks to the deployed API. A Vercel
# dashboard env var, if set, overrides this.
NEXT_PUBLIC_API_URL=https://vertxing.onrender.com
