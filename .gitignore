# Vertxing — Real-Time Video Meeting Platform
# ─────────────────────────────────────────────────────────────────────────────
# File:    .gitignore
# Purpose: Keep build artifacts, secrets, and dependencies out of version control.
# ─────────────────────────────────────────────────────────────────────────────

# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Environment & secrets (the *.example templates stay tracked)
.env
.env.local
.env.*.local
.env.production
**/.env.production
# Exception: the web's prod env holds only the PUBLIC API URL (no secret), and
# Vercel needs it at build time — so track this one.
!apps/web/.env.production

# Prisma generated client
apps/api/src/infrastructure/prisma/generated/

# Logs
logs/
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage/
.nyc_output/
